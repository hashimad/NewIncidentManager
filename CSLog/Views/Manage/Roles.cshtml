@model IEnumerable<Microsoft.AspNet.Identity.EntityFramework.IdentityRole>
@{
    ViewBag.Title = "Roles";
}

<div>
    <a class="btn btn-success btn-sm pull-right" href="@Url.Action("AddRole", "Manage")" data-modal=""><i class="fa fa-plus"></i> Add Role</a>
    @*<h4 class="title">Add New Role</h4>
        <hr />
        <div class="row">
            <div class="col-md-4">
                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()
                    <div class="form-horizntal">

                        @Html.Raw(ViewBag.Msg)
                        <div class="form-group">
                            <label class="custom-control-label col-md-12"></label>
                            <div class="col-md-12">
                                <input type="text" placeholder="Name" name="Name" class="form-control" />
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-md-12">
                                <input type="submit" value="Add" class="btn btn-danger btn-block sm-postr" />
                            </div>
                        </div>
                    </div>
                }
            </div>
            <div class="col-md-8">

            </div>
        </div>*@
        
    <div class="card-sm effect p30">
        <h4 class="title">List Of Roles</h4>
        @Html.Raw(ViewBag.Msg)
        <table class="table table-condensed table-bordered">
            <thead>
                <tr>
                    <th>Role Name</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            <div class="dropdown open pull-right">
                                <button class="btn btn-primary btn-sm dropdown-toggle" type="button" id="triggerId" data-toggle="dropdown" aria-haspopup="true"
                                        aria-expanded="false">
                                    Action
                                </button>
                                <div class="dropdown-menu" aria-labelledby="triggerId">
                                    <a class="dropdown-item getr" href="/Manage/RoleDetails/@item.Id">Details</a>
                                    @*<a class="dropdown-item" href="/Manage/EditRole/@item.Id" data-modal="">Edit</a>*@

                                </div>
                            </div>
                        </td>

                    </tr>
                }
            </tbody>
        </table>
    </div>


</div>

